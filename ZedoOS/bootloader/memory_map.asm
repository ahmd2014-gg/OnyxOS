[bits 16]
[global]

memory_map:
    ; استكشاف مناطق الذاكرة
    ; مثال على التحقق من الذاكرة القابلة للاستخدام
    mov ax, 0xE820       ; استدعاء المقاطعة 0x15 للحصول على خريطة الذاكرة
    mov ebx, 0           ; صفر EBX لاستخدامه في المكالمة
    mov ecx, 24          ; حجم البيانات في الخريطة
    int 0x15             ; المقاطعة 0x15 لاسترجاع خريطة الذاكرة
    ; إضافة المزيد من الكود لتحليل الخريطة هنا
    ret
